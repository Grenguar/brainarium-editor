{
  "mcpServers": {
    "brainarium-vault": {
      "type": "stdio",
      "command": "docker",
      "args": [
        "run",
        "--rm",
        "-i",
        "-e",
        "BRAINARIUM_VAULT=/vault",
        "-e",
        "BRAINARIUM_MCP_ALLOW_WRITE=false",
        "-v",
        "/absolute/path/to/the-same-vault-selected-in-Brainarium:/vault:ro",
        "brainarium-mcp:local"
      ]
    }
  }
}
